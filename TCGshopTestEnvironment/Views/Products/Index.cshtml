@model TCGshopTestEnvironment.ViewModels.ProductsIndexModel
@{ViewBag.Title = "Product View";
}

@using X.PagedList.Mvc; 
@using X.PagedList;
@using X.PagedList.Mvc.Core

<div id="asssets">
    <h3>Product</h3>
    <div id="productsTable">
        @Html.PagedListPager((IPagedList)ViewBag.OnePageOfProducts, page => Url.Action("Index", new { page, ViewBag.PageAmount }))
        <table class="table table-condensed" id="productsIndexTable">
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Name</th>
                    <th>Grade</th>
                    <th>Stock</th>
                    <th>Price</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var asset in ViewBag.OnePageOfProducts)
                {
                    <tr class="assetRow">
                        <td class="">
                            <a asp-controller="Products" asp-action="Detail" asp-route-id="@asset.Id">
                                <img src="/images/TCG-Cards/@(asset.ImageUrl + ".png")" class="imageCell" />
                            </a>
                        </td>
                        <td class="">@asset.Name</td>
                        <td class="">@asset.Grade</td>
                        <td class="">@asset.Stock</td>
                        <td class="">€@asset.Price</td>

                    </tr>
                }
            </tbody>
        </table>
        @Html.PagedListPager((IPagedList)ViewBag.OnePageOfProducts, page => Url.Action("Index", new { page, ViewBag.PageAmount }))
    </div>
    <a asp-controller="Products" asp-action="Index" asp-route-page="@ViewBag.page" asp-route-pageAmount="5"><button>5</button></a>
    <a asp-controller="Products" asp-action="Index" asp-route-page="@ViewBag.page" asp-route-pageAmount="10"><button>10</button></a>
    <a asp-controller="Products" asp-action="Index" asp-route-page="@ViewBag.page" asp-route-pageAmount="20"><button>20</button></a>
    <a asp-controller="Products" asp-action="Index" asp-route-page="@ViewBag.page" asp-route-pageAmount="50"><button>50</button></a>
</div>